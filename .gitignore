# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Build output
client/dist/

# Data directory (runtime state — seeded automatically on first start)
data/

# Local environment config
.env
*.sh

# SSH keys — never commit
*_key
*_rsa
*.pem

# OS / IDE
.DS_Store
.idea/
*.iml
/data_backup/
/remoteBoard/
/remoteData/
/run-remote-local.sh
